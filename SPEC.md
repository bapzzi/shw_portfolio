# SPEC — 포트폴리오 재정의 (2026-08-12 · Fable 검토 반영 v2)

> 1회성 문서. 구현 완료 후 아카이브(`docs/standards/webapp-docs-set.md`).
> 전제 = 2026-07-10 오너 판정("원하는 포트폴리오와 멀다")로 동결된 상태의 해제.
> v2 = 같은 날 Fable 적대 검토 15건 반영(P0 부반장 모순·경로 오류·계층 충돌 해소).

---

## 0. 이 개편이 푸는 문제

**진단**: 동결의 원인은 두 가지였고, 이번에 푸는 것은 ②다.

| # | 문제 | 이번 범위 |
|---|---|---|
| ① | 결과물의 "느낌"이 원하는 것과 다름 | **범위 밖** — 디자인 토큰·타임라인 시그니처는 유지. 개편 후에도 어긋나면 별도 패스 |
| ② | **담긴 것이 실제 한 일의 일부뿐** — 케이스 3건에서 멈춤 | **이번 범위** |

**②의 실체**: 2026-07-10 이후 생긴 것이 하나도 반영돼 있지 않다 — VS Code 확장 마켓플레이스 정식 배포, AIM 허브(백엔드·CI·자동 발행), 공개 TIL 체계, ADsP 1기 완주. AIM 프로젝트 페이지는 '스터디가 어떻게 굴러갔나'를 쓰는 자리라 '신해원이 무엇을 할 수 있나'가 구조적으로 안 담긴다(오너: "AIM에 작성하는 것만으로는 부족").

## 1. 확정 요구 (오너, 2026-08-12)

| 항목 | 값 |
|---|---|
| 형태 | **기존 웹앱 재정의** — `shw-portfolio.vercel.app` 유지, 디자인 유지, 구조·콘텐츠 교체 |
| 독자 | **4종 전부** — ①DX/AM 직무 채용담당 ②LG CNS 캠프 평가·추천서 ③AIM 모집·교내 ④본인 정리용 |
| 수록 범위 | **전량 축적** — "일단 다 쌓아두고 뺄 부분은 내가 나중에 조립식으로 제거" |
| 서술 원칙 | **동등 깊이 + 타임라인 순**(recency bias 금지) — §1-1 |
| 캠프 경계 | **공개본만**(TIL·마켓 확장) — §8 |

**설계를 결정하는 문장**: 독자가 4종인데 수록은 전량이다. 한 화면에 전부 쏟으면 어떤 독자에게도 안 맞는다.
→ **축적(원천)과 노출(화면)을 분리한다.** 빼는 행위는 삭제가 아니라 **메타데이터 한 줄 수정**이어야 한다.

### 1-1. 동등 깊이·타임라인 원칙 (recency bias 금지)

- **문제 인식(오너)**: 포폴을 쓸 때마다 최근 내용이 더 중요한 것처럼 다뤄지는 편향이 반복됐다. 이 포폴은 **아카이브**다 — 지금까지의 내용을 잊지 않게 만들어 두는 것이 목적.
- **규칙 1 — 깊이는 사료(史料)의 양이 정한다. 시기·화제성이 아니다.** 1차 기록(커밋·문서·수치·산출물)이 남은 소재 = **케이스**(전개형), 진술·기억뿐인 소재 = **워크**(요약형). 오래된 소재라고 자동으로 워크가 되는 게 아니라, **사료를 복원할 수 있으면 케이스로 승격**한다. 사료가 부족해 워크에 머무는 경우 그 사실을 원고에 명기한다("기록 부족으로 ~는 복원 불가").
- **규칙 2 — 케이스 계층 안에서는 동등 깊이.** 모든 케이스는 같은 골격(요약표·**「숫자로 본 결과」 표**·타임라인·결정·⊘기각·배운 것)으로 쓴다. 최근 소재라고 절을 늘리지 않는다. 「숫자로 본 결과」 = 구분|수치|비고 3열 표, 전 행 실측·출처 명기(오너 2026-08-12 — "수치 기반으로 정리". 첫 적용 = adsp-study).
- **규칙 3 — 정렬·집필 모두 과거부터.** 목록 기본 정렬 = 시작 시점 오름차순. '최신순' 기본값 금지. 집필 순서도 오래된 것부터(사료가 휘발되기 전에 굳힌다).

## 2. 핵심 결정 — 3계층 콘텐츠 모델

```
content/
├── cases/*.md      전개형 — 타임라인·결정·⊘기각·수치 (현 케이스 형식 유지)
├── works/*.md      요약형 — 카드 1장(문제·역할·결과·링크). 사료 확보 시 케이스 승격
└── assets.md       사실 목록 — 항목 단위 표(서술 없음)
```

- `works/`는 `career/cover-letter-db/experiences/`에서 **복사가 아니라 파생**(원천 = letter-db 유지).
- **중복 등재 금지**: 케이스로 다룬 소재는 워크에 다시 만들지 않는다(§4-2에서 해당 소재 제외).
- `journey.md`·`how.md`는 이 계약 **밖**(현행 로딩 유지). 잔재 `content/portfolio.md`는 C0에서 삭제.

### frontmatter 계약 (cases·works 공통)

```yaml
---
슬러그: lesson-replay
제목: 수업 리플레이 — VS Code 확장     # 표시 원천 = frontmatter (H1은 본문용, 파서는 frontmatter 우선)
태그라인: 수업을 자동 기록해 복기로 되돌리는 확장
기간: 2026-08 ~                # YYYY-MM 최소 단위. 시작 결측 금지(연도만 알면 YYYY-01). 종료 결측 = 진행 중
분류: 제품                     # 제품 | 운영 | 학습 | 조직 — 카드 뱃지 표시용
독자: [채용, 캠프, 교내]        # enum {채용, 캠프, 교내, 본인}. 빈 배열 = 어느 공개 프리셋에도 안 뜸
노출: true                    # false = 화면 전체에서 제거(본인 프리셋 제외)
---
```

- 정렬 키 = `기간` 시작(YYYY-MM) 오름차순. 별도 우선순위 필드 없음.
- **`assets.md`는 항목 단위 표** — 열 = 항목 | 값·근거 | 독자. 파서가 행 단위로 프리셋 필터(파일 단위 아님 — "한 줄 제거" 원칙을 자산에도 적용).
- ⚠ **노출: false여도 원고는 정적 번들에 포함된다**(글롭 로딩 + `?for=본인` URL은 추측 가능). 따라서 **노출값과 무관하게 §5·§8 게재 경계를 통과한 내용만** content/에 넣는다. 경계 밖 내용의 보관처는 letter-db(비공개 repo).

## 3. 화면 — 독자 프리셋

| 프리셋 | URL | 필터식 |
|---|---|---|
| 채용(기본) | `/?for=recruit` | `노출 ∧ '채용'∈독자` |
| 캠프 | `/?for=camp` | `노출 ∧ '캠프'∈독자` |
| 교내 | `/?for=campus` | `노출 ∧ '교내'∈독자` |
| 전체 | `/?for=all` | `노출` (독자 무관) |
| 본인 | `/?for=self` | 무필터 — `노출: false` 포함 전량 |

- 쿼리값 = **영문**(URL 복사·공유 시 percent-encoding 방지). 화면 라벨은 한글.
- 프리셋은 필터일 뿐 별도 페이지가 아니다. 홈에 전환 UI는 채용·캠프·교내·전체 4개만 노출, `self`는 **링크 없는 숨김**(잠정 — §9-2 오너 결정으로 로컬 전용 전환 가능).
- 프리셋 안 정렬 = §1-1 규칙 3(시작 시점 오름차순).

## 4. 수록 대상 (초기 적재 목록)

> 전량 축적 원칙에 따라 전부 만든다. 뺄지는 만든 뒤 오너가 `노출`·`독자`로 결정.
> **집필 순서 = 시작 시점 오름차순**(§1-1 규칙 3). 아래 표도 그 순서다.

### 4-1. 케이스(전개형)

| 슬러그 | 시작 | 무엇 | 상태 |
|---|---|---|---|
| `harness` | 2026-05경 | Claude Code 하네스 운영 — 헌법·훅·가드·스킬·메모리·3기기 동기화 | **신규** ★차별점의 뿌리 |
| `adsp-study` | 2026-06-26 | 스터디 운영 8명·6주 — 데이터로 운영을 판단(보드와 분리) | **신규** |
| `adsp-board` | 2026-06-28 | 진도 보드 — 지표 기준을 체감에 맞춰 재정의 | 기존(1기 종결 결과로 갱신) |
| `ai-relay` | 2026-07초 | 요청 릴레이 웹앱 | 기존 |
| `portfolio` | 2026-07-10 | 이 사이트 자체 | 기존 |
| `aim-hub` | 2026-07-22 | AIM 허브 — 정적 MPA + Supabase 워크스페이스, CI 3중 게이트, 자동 발행 루틴 | **신규** ★규모 최대 |
| `lgcns-til` | 2026-07-24 | 공개 TIL 체계 — 백지 인출·갭 원장·발행 파이프라인 (공개본 한정 §8) | **신규** |
| `lesson-replay` | 2026-08-02 | VS Code 확장 — 마켓 정식 배포 v1.0→v1.3, 실사용 버그 반복 수리 | **신규** ★외부 배포·타인 검증 |

### 4-2. 워크(요약형) — letter-db 파생, 시작 시점 순

요리→대학 · 군 재고 엑셀 자동화 · KAPA 총무 · 글로벌 챌린저 · ADIT SNS · ERP연구회 회장 · SAP MM 구축 · ABAP ALV 리포트 · ABAP Cloud 스터디 리드
(제외 = `ai-harness-mastery`→케이스 `harness`와 중복, `vibe-coding-automation`→케이스 3건과 중복. §2 중복 등재 금지)

### 4-3. 자산(사실 목록 — assets.md 행 단위)

배포 URL 4종 · VS Code 마켓플레이스 확장 1종 · SAP 자격증 2종(유효기간 포함) · 학점 3.94 · LG CNS AM 캠프 6기 수료 과정 · 광운대 경영학부 4학년
(⚠ 부반장 등 캠프 내 역할은 **게재 금지** — §8. v1에서 자산 목록에 있던 것을 Fable 검토 P0로 제거)

### works 원고 골격 (C2 검증·C4 렌더 기준)

```markdown
---
(frontmatter 계약 동일)
---
# 제목 — 태그라인
- **문제**: 한 줄
- **역할**: 한 줄
- **결과**: 수치·산출물(출처 병기)
- **링크**: 있으면 (없으면 생략)
- **사료**: 남은 근거 한 줄("기록 부족으로 ~ 복원 불가" 명기 지점)
```

## 5. 사실성 규칙 (강제)

1. **모든 수치는 출처 병기**(커밋·DB 실측·릴리스). 손으로 쓴 어림수 금지.
2. **홈 통계는 원고에서 집계.** 집계 원천 표: `nodes`·`decisions`·`rejected` = cases 파싱 / `live` = assets.md 배포 URL 행 수(**현행 `live: 3` 하드코딩은 C1에서 제거** — 기존 위반 해소).
3. **과장 어휘 금지** — erp-club R5 목록 준용.
4. **타인 정보 비게재** — 실명·개인 단위 수치 금지, 집계만. **부분집합 크기 1인 집계 금지**(예: "활성 1/8명" — 내부자 특정 가능). 검증 명단 원천 = `adsp/1기-데이터/멤버별-요약.md` 이름 열 + ai-relay 사용자.
5. **미확정은 쓰지 않는다** — 슬롯만 두고 공란.
6. **교재·저작물 서술 순화(오너 2026-08-12)** — "교재 단일원천"·"교재 스캔 N장 판독" 류 표현 금지(저작권 우려 + 중요 결정 아님으로 판정). 표준 표현 = **"교재를 직접 학습하며 요약한 내용을 토대로 제작"**. erp-club 8/6 저작권 순화와 같은 구도.

## 6. 구현 범위 (코드)

| # | 작업 | 파일 |
|---|---|---|
| **C0** | **콘텐츠 이관** — 기존 case-*.md → `content/cases/`로 이동+frontmatter 소급 부여, 잔재 `content/portfolio.md` 삭제, journey/how는 계약 제외 확인 | `content/` |
| C1 | 글롭 로딩 — `import.meta.glob('../../../content/{cases,works}/*.md', { query: '?raw', import: 'default', eager: true })` + assets.md 로더 + `stats.live` 하드코딩 제거 | `src/lib/content.ts` |
| C2 | frontmatter 파서(현 `parseDoc` 앞단) + **계약 위반 시 빌드 실패**(대상 = cases·works만) | `src/lib/parse.ts` |
| C3 | 프리셋 상태·URL 쿼리(`?for=`, 영문 값) + 시작일 오름차순 정렬 | `src/pages/Home.tsx` |
| C4 | 요약형(works) 카드 + 썸네일 규약 — frontmatter `커버`(선택, 없으면 텍스트 카드). 현 `thumbs` 하드코딩 3건 → `커버`로 이관 | `src/components/` 신규 |
| C5 | 자산 섹션 렌더(행 단위 필터) | `src/pages/Home.tsx` |
| C6 | 테스트 — §7 전 항목 | `*.test.ts` |

- **300줄 규격** 준수. 디자인 토큰·컴포넌트는 건드리지 않는다(범위 밖).

## 7. 완료 기준 (T/F)

- [ ] 케이스 8건 + 워크 9건 + assets.md가 `content/`에 존재
- [ ] `노출: false` 한 줄로 항목이 사라지고 되돌리면 복구 (테스트)
- [ ] `?for=recruit/camp/campus`가 서로 다른 목록 (테스트)
- [ ] `?for=all` = 노출 전량 / `?for=self` = 노출:false 포함 전량 (테스트)
- [ ] 목록이 시작일 오름차순으로 정렬 (테스트)
- [ ] frontmatter 계약 위반 파일 투입 시 빌드 FAIL (negative 테스트)
- [ ] 홈 통계 수치 하드코딩 0 (`live: 3` 제거 확인 포함)
- [ ] 빌드·lint·테스트 GREEN · 3뷰포트(375/768/1440) 가로 오버플로우 0
- [ ] 타인 실명 0건 — §5-4 명단 원천 기준 grep + 부분집합 크기 1 집계 0건

## 8. 캠프 산출물 게재 경계 (오너 확정 2026-08-12)

**공개본만 게재한다.** 이미 외부에 공개된 것에 한정:

| 게재 가능 | 게재 금지 |
|---|---|
| 티스토리 발행 TIL(공개 URL 존재) | 강사 배포 소스(실명·이메일 포함) |
| VS Code 마켓플레이스 확장(공개 배포물) | 캠프 내부 자료·공지·시간표 |
| 공개 repo(`bapzzi/lesson-replay`·TIL repo) | 반·동기 관련 정보(이름·평가·진도) |
| | **부반장 등 운영 역할·모니터링 리포트** |

- `lgcns-til` 케이스는 **공개 TIL과 그 제작 체계**만 다룬다 — 캠프 내부 운영은 서술하지 않는다.
- 이 경계는 `노출: false` 원고에도 동일 적용(§2 — 번들에 포함되므로).
- 기존 원칙과 일관: erp-club 규칙 "운영자 개인 이력(캠프 등) 대외 표면 금지".

## 9. 열린 항목 (오너 결정 대기)

1. ~~원고 작성 순서~~ → **확정(§1-1 규칙 3): 오래된 것부터.**
2. **`self` 프리셋의 공개 여부** — 배포본 숨김(잠정, §3) vs 로컬 전용. ⚠단 §2 규칙상 어느 쪽이든 경계 밖 내용은 못 담는다.
3. **letter-db 연동 수준** — 수동 파생 vs 스크립트 생성. 권고 = **수동**(9건뿐이고 문체가 다름).
4. **`harness` 케이스의 시작 시점 확정** — 하네스 운용 개시일(오너 기억 필요, §4-1은 2026-05경 추정).
