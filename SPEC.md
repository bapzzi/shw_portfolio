# SPEC — 포트폴리오 재정의 (2026-08-12)

> 1회성 문서. 구현 완료 후 아카이브(`docs/standards/webapp-docs-set.md`).
> 전제 = 2026-07-10 오너 판정("원하는 포트폴리오와 멀다")로 동결된 상태의 해제.

---

## 0. 이 개편이 푸는 문제

**진단**: 동결의 원인은 두 가지였고, 이번에 푸는 것은 ②다.

| # | 문제 | 이번 범위 |
|---|---|---|
| ① | 결과물의 "느낌"이 원하는 것과 다름 | **범위 밖** — 디자인 토큰·타임라인 시그니처는 유지. 개편 후에도 어긋나면 별도 패스 |
| ② | **담긴 것이 실제 한 일의 일부뿐** — 케이스 3건에서 멈춤 | **이번 범위** |

**②의 실체**: 2026-07-10 이후 생긴 것이 하나도 반영돼 있지 않다 — VS Code 확장 마켓플레이스 정식 배포, AIM 허브(백엔드·CI·자동 발행), LG CNS 캠프(TIL 체계·부반장), ADsP 1기 완주. 오너 표현 "AIM에 작성하는 것만으로는 부족" = **AIM 프로젝트 페이지는 '스터디가 어떻게 굴러갔나'를 쓰는 자리라 '신해원이 무엇을 할 수 있나'가 구조적으로 안 담긴다.**

## 1. 확정 요구 (오너, 2026-08-12)

| 항목 | 값 |
|---|---|
| 형태 | **기존 웹앱 재정의** — `shw-portfolio.vercel.app` 유지, 디자인 유지, 구조·콘텐츠 교체 |
| 독자 | **4종 전부** — ①DX/AM 직무 채용담당 ②LG CNS 캠프 평가·추천서 ③AIM 모집·교내 ④본인 정리용 |
| 수록 범위 | **전량 축적** — "일단 다 쌓아두고 뺄 부분은 내가 나중에 조립식으로 제거" |

**설계를 결정하는 문장**: 독자가 4종인데 수록은 전량이다. 한 화면에 전부 쏟으면 어떤 독자에게도 안 맞는다.
→ **축적(원천)과 노출(화면)을 분리한다.** 빼는 행위는 삭제가 아니라 **메타데이터 한 줄 수정**이어야 한다.

## 2. 핵심 결정 — 3계층 콘텐츠 모델

```
content/
├── cases/*.md      전개형 — 타임라인·결정·⊘기각·수치 (현 케이스 형식 유지)
├── works/*.md      요약형 — 카드 1장 분량(문제·역할·결과·링크)
└── assets.md       사실 목록 — 배포 URL·수치·자격증 (서술 없음)
```

- **깊이가 다른 것을 같은 틀에 넣지 않는다.** 현 앱은 전개형 하나뿐이라, 얕은 재료를 넣으면 빈 타임라인이 된다.
- `works/`는 `career/cover-letter-db/experiences/`(11건)에서 **복사가 아니라 파생**한다 — 원천은 letter-db 유지(3계층 설계 §portfolio-system-progress).

### frontmatter 계약 (전 계층 공통)

```yaml
---
슬러그: lesson-replay
제목: 수업 리플레이 — VS Code 확장
태그라인: 수업을 자동 기록해 복기로 되돌리는 확장
기간: 2026-08-02 ~ 진행 중
분류: 제품            # 제품 | 운영 | 학습 | 조직
독자: [채용, 캠프, 교내]   # 이 항목을 볼 독자 (빈 배열 = 아무 프리셋에도 안 뜸)
노출: true            # false = 원고는 남기고 화면에서만 제거  ← "조립식 제거"의 스위치
우선순위: 10          # 낮을수록 위. 프리셋 안 정렬 기준
---
```

- **"빼기" = `노출: false` 또는 `독자` 배열에서 제거.** 파일 삭제·코드 수정 없음. 되돌리기도 한 줄.
- `독자` enum = `채용` `캠프` `교내` `본인`. 값 추가는 오너 승인 사항(임의 확장 금지).

## 3. 화면 — 독자 프리셋

- 홈에 **프리셋 전환 4개**(채용·캠프·교내·전체). 기본 = `채용`.
- URL로 고정 가능: `/?for=캠프` — **링크를 보낼 때 상대가 볼 화면을 지정**할 수 있어야 한다(추천서·모집 공유 시 필요).
- 프리셋은 **필터일 뿐 별도 페이지가 아니다.** 라우트·상세 페이지는 그대로.
- `본인` 프리셋 = `노출: false` 포함 전량 표시(비공개 확인용). **배포본에서도 접근 가능하되 링크 없음**(숨김 프리셋).

## 4. 수록 대상 (초기 적재 목록)

> 전량 축적 원칙에 따라 **아래를 전부 만든다.** 어느 것을 뺄지는 만든 뒤 오너가 결정.

### 4-1. 케이스(전개형) — 8건

| 슬러그 | 무엇 | 상태 |
|---|---|---|
| `lesson-replay` | VS Code 확장 — 마켓플레이스 정식 배포, v1.0.0→v1.3.1, 실사용 버그 반복 수리 | **신규** ★외부 배포·타인 검증 |
| `aim-hub` | AIM 허브 — 정적 MPA + Supabase 워크스페이스(로그인·기고·승인·공고·캘린더), CI 3중 게이트, 자동 발행 루틴 | **신규** ★규모 최대 |
| `adsp-board` | 진도 보드 — 지표 기준을 체감에 맞춰 재정의 | 기존(1기 종결 결과로 갱신) |
| `adsp-study` | 스터디 운영 8명·5주 — 데이터로 운영을 판단 | **신규**(보드와 분리) |
| `harness` | Claude Code 하네스 운영 — 헌법·훅·가드·스킬·메모리·3기기 동기화 | **신규** ★차별점의 뿌리 |
| `lgcns-til` | 캠프 학습 체계 — 백지 인출·갭 원장·발행 파이프라인 | **신규** |
| `ai-relay` | 요청 릴레이 웹앱 | 기존 |
| `portfolio` | 이 사이트 자체 | 기존 |

### 4-2. 워크(요약형) — letter-db 11건 파생

SAP MM 구축 · ABAP ALV 리포트 · ABAP Cloud 스터디 리드 · 군 재고 엑셀 자동화 · KAPA 총무 · 글로벌 챌린저 · ADIT SNS · 요리→대학 · ERP연구회 회장 · 바이브코딩 자동화 · AI 하네스 숙련

### 4-3. 자산(사실 목록)

배포 URL 4종 · VS Code 마켓플레이스 확장 1종 · SAP 자격증 2종(유효기간 포함) · 학점 3.94 · LG CNS AM 캠프 6기(부반장) · 광운대 경영학부 4학년

## 5. 사실성 규칙 (강제)

1. **모든 수치는 출처가 있어야 한다.** 원고에 근거(커밋·DB 실측·릴리스)를 병기. 손으로 쓴 어림수 금지.
2. **홈 통계는 원고에서 집계**한다(현행 `stats` 방식 유지·확장). 별도 하드코딩 금지.
3. **과장 어휘 금지** — 기존 erp-club R5 목록 준용(전문적·깔끔·완벽·최대한 등).
4. **타인 정보 비게재** — 스터디원 실명·개인 단위 수치 금지(ADsP 1기 아카이브 규칙과 동일). 집계만.
5. **미확정은 쓰지 않는다** — 시험 성적(8/28 발표) 등 미확정 값은 슬롯만 두고 공란.

## 6. 구현 범위 (코드)

| # | 작업 | 파일 |
|---|---|---|
| C1 | 하드코딩 import → **글롭 로딩**(`import.meta.glob('../../content/**/*.md')`) | `src/lib/content.ts` |
| C2 | **frontmatter 파서** 추가(현 `parseDoc` 앞단) + 계약 위반 시 빌드 실패 | `src/lib/parse.ts` |
| C3 | 프리셋 상태·URL 쿼리(`?for=`) | `src/pages/Home.tsx` |
| C4 | 요약형(`works`) 카드 컴포넌트 — 전개형과 다른 렌더 | `src/components/` 신규 |
| C5 | 자산 섹션 렌더 | `src/pages/Home.tsx` |
| C6 | 테스트 — 노출 토글·프리셋 필터·frontmatter 검증 | `*.test.ts` |

- **300줄 규격** 준수(size-guard). `content.ts`가 커지면 로더/집계 분리.
- 디자인 토큰·컴포넌트는 **건드리지 않는다**(범위 밖 명시).

## 7. 완료 기준 (T/F)

- [ ] 케이스 8건 + 워크 11건 + 자산 목록이 `content/`에 존재한다
- [ ] `노출: false` 한 줄로 항목이 화면에서 사라지고, 되돌리면 복구된다 (테스트로 고정)
- [ ] `/?for=채용` `/?for=캠프` `/?for=교내`가 서로 다른 목록을 보여준다
- [ ] 홈 통계 수치가 전부 원고에서 계산된다(하드코딩 0)
- [ ] 빌드·lint·테스트 GREEN · 3뷰포트(375/768/1440) 가로 오버플로우 0
- [ ] 타인 실명·개인 단위 수치 0건 (grep 검증)

## 8. 열린 항목 (오너 결정 대기)

1. **원고 작성 순서** — 신규 케이스 6건을 한 세션에 몰아 쓸지, 강한 것부터(lesson-replay → aim-hub → harness) 나눠 쓸지.
2. **`본인` 프리셋의 공개 여부** — 배포본에 숨김으로 둘지, 로컬 전용으로 뺄지.
3. **letter-db 연동 수준** — 수동 파생(복사 후 손질) vs 스크립트 생성. 현 시점 권고 = **수동**(11건뿐이고 문체가 다름).
4. **캠프 산출물 게재 범위** — TIL은 공개 블로그지만, 캠프 내부 자료·강사 배포 소스는 게재 불가. 경계선 확인 필요.
