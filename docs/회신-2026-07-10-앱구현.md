# 회신 — 포트폴리오 앱 구현 (브리프 2026-07-10)

> 실행 세션 → 통제 세션. 브리프 `docs/브리프-2026-07-10-앱구현.md` ⑤ 회신 계약 8항목.

## 1. 커밋 목록 + push

| 해시 | 요약 |
|------|------|
| 29b3d15 | chore: 정적 페이지 은퇴(v1 태그 보존) — html 6·css·js 8파일 삭제 |
| 884f1ae | docs: 승인 시안(다크+옐로) repo 보존 — 디자인규칙 지시 |
| a7fcd1f | feat(app): Vite+React+TS 스캐폴드 + 디자인 토큰·폰트·SPA rewrites |
| 5094618 | feat(app): content 파서 + 버전 타임라인 시그니처 + 케이스 상세 3페이지 |
| 16d635a | feat(app): 홈(히어로·수치·3카드·여정·About·Contact) + /how + 라우팅 |
| ebd96ec | docs: 3뷰포트 검증 캡처 9장 + README 3칸·작업기록 현행화 |

- **push 완료**(origin/main, 8ef7d9b..ebd96ec). 이 회신서 커밋은 표 이후 추가.
- 태그: `v1-internet-course` **기존 존재 확인** → 신규 생성 없이 진행(브리프 조건 충족).

## 2. 3뷰포트 캡처 (docs/shots/v2/)

홈·케이스1(adsp-board)·/how × 390/1280/1920 = 9장:
`home-390/1280/1920.png` · `case-adsp-board-390/1280/1920.png` · `how-390/1280/1920.png`

## 3. 빌드·lint

- `npm run build`(tsc -b && vite build): **통과, 경고 0** — index.html 1.12kB · css 9.34kB(gzip 2.55) · js 263.28kB(gzip 85.81)
- `npm run lint`(oxlint): **0건**

## 4. 링크 전수 확인

| 구분 | 대상 | 결과 |
|------|------|------|
| 내부 | `/` `/case/adsp-board` `/case/ai-relay` `/case/portfolio` `/how` | **OK** — 5라우트×3뷰포트 15/15 렌더 확인, 가로 오버플로우 0 |
| 내부 | 미존재 경로(`*`) | 홈으로 리다이렉트 |
| 외부 | https://erpstudy.vercel.app | **200 OK** (홈 카드·케이스 요약에서 링크) |
| 외부 | https://ai-relay-sigma-two.vercel.app | **200 OK** (홈 카드·케이스 요약에서 링크) |
| 외부 | https://kwu-erpclub.github.io | **200 OK** — 단, ⚠ **원고 어디에도 이 링크가 없어 사이트에 미노출**(클릭 확인 대상 부재, URL 자체만 확인) |
| 기타 | mailto:win737449@gmail.com | 표기 확인 |

## 5. 오탈자·원고 이슈 (수정하지 않음)

1. `case-portfolio.md` 요약 "라이브 | (구현 후 기입)" — 플레이스홀더가 화면에 그대로 노출됨
2. `case-portfolio.md` 마지막 노드 "구현 → 배포 (예정)"의 "(구현 세션 완료 후 실측 기입…)" 괄호 문안이 그대로 렌더됨
3. `case-ai-relay.md` 첫 노드 결과의 "⊘기각 자유 텍스트 입력"이 **인라인**이라 기각 곁가지(스탬프)가 아닌 본문 텍스트로 렌더됨 — 별도 불릿 분리 여부 판단 요망
4. `journey.md`의 `원천:` 필드는 내부 포인터로 보고 **화면 미노출** 처리함(원고 자체는 무변경)
5. 브리프 ⑦의 외부 링크 kwu-erpclub.github.io가 원고 4건 어디에도 없음(위 4번 표 참조)
6. 순수 오탈자는 발견하지 못함
- **content/ 기존 원고 4건 diff 0** (git status로 확인)

## 6. case-portfolio.md 실측 수치 후보 (원고에 쓰지 않음)

- 이번 구현 커밋 **7**(은퇴1·시안1·feat3·docs1·회신1) / 원고 무변경 diff 0
- 검증: 5라우트×3뷰포트 **15/15 렌더·가로 오버플로우 0**, 캡처 9장
- 빌드 gzip JS **85.81kB**, 빌드 경고 0, lint 0
- 원고 집계(홈 수치 밴드 표시값): 분기점 **16** · 기록된 결정 **13** · 기각·보류 **11**
- 라이브 URL: Vercel 연결 후 확정(사용자 게이트)

## 7. Vercel 연결 안내 (사용자 실행)

1. vercel.com 로그인(GitHub 계정 bapzzi)
2. **Add New… → Project** → Import Git Repository에서 `bapzzi/shw_portfolio` 선택
3. **Root Directory를 `app`으로 변경**(Edit 버튼) — Framework Preset은 Vite 자동 인식
4. Build Command `npm run build` / Output Directory `dist` — 기본값 그대로 두고 **Deploy**
5. 배포 완료 후 발급 도메인 확인 → 주소창에서 `/case/adsp-board` 직접 입력·새로고침으로 SPA rewrites(vercel.json) 동작 확인
6. 라이브 URL을 통제 세션에 전달(case-portfolio.md 실측 기입용)

## 8. 브리프에서 벗어난 판단 (전부)

1. **content/how.md 신규 작성(v0)** — 브리프에 /how 원고가 없어, 기존 승인 문서 문장만 큐레이션해 초안 작성(검토 대기 표기). 승인·수정·기각 요망
2. 히어로·섹션 헤드라인·CTA 문안 = **승인 시안에서 이관**(컴포넌트 내 UI 카피, content/ 파일 아님)
3. About·Contact 데이터(학력·SAP 자격 2종·수상 2·이메일 1) = journey 표기 규칙 + cover-letter-db profile **공개 범위만** 컴포넌트에 기입(인적사항 미노출)
4. 홈 수치 밴드 = 시안 더미 수치(3/47/21/312) 대신 **원고 집계값**(라이브 3·분기점 16·결정 13·기각 11) — 부풀린 수치 금지 원칙 적용
5. 시안 HTML의 **고스트 워터마크·blob은 제외**(§5 금지 목록이 시안보다 우선)
6. 카드 썸네일 = 라이브 스크린샷 대신 **시안 CSS 목업** — §0 "카드=스크린샷" 미충족 상태, 스크린샷 교체는 백로그로 남김
7. 캡처 도구: claude-in-chrome 권한 거부(3회, 사용자 허용 후에도)로 **puppeteer-core**(`--no-save` 임시 설치, repo 미기록)로 대체. 검증 스크립트는 커밋하지 않음
8. 승인 시안 repo 보존(`docs/시안/`) — 디자인규칙 머리말 지시 이행(브리프 절차에는 없던 항목)
9. README 3칸·작업기록.md 현행화 커밋 — erp-club 작업 원칙 1(현행화) 준수
10. 여정 h2의 "AI" 1단어 옐로 강조 — §3 헤드라인 규칙 적용(문안 무변경, 표현만)
11. 스캐폴드 템플릿 favicon 삭제 → **파비콘 없음**(백로그)
12. journey `원천:` 필드 화면 미노출(위 5-4)
